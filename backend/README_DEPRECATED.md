# Deprecated Backend Folder

This folder is no longer an independent backend implementation.

It is retained only for compatibility to avoid breaking old imports.

## Source of Truth
- Canonical backend runtime: `main.py`
- Canonical models/schemas/database: project root modules

## Important
- Do not deploy this folder as a standalone backend.
- Legacy modules here now proxy to canonical root modules.
